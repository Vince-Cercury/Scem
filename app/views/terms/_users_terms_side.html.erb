<div id="results">
  <% @terms.each do |term| %>
    <% event=term.event %>

      <%=  display_term_box(term.start_at, term.end_at) %><br />
      <%= display_event_cover(event, :thumb) %>
    <%= link_to(event.name, event) %>
  <% end %>
</div>
