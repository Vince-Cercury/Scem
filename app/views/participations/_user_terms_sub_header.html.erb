<!--<% if @category.nil? %><h3 class="titleevent"><%= t("events.listing_events") %></h3><% end %>-->


<div class="listParticipation">
<ul class="tabsevent">
  <li>
    <% if params[:period]=='past'%>
      <%= link_to t('terms.next_events_coming'), user_participations_path(:period => 'futur') %>
    <% else %>
      <%= link_to(t('terms.next_events_coming'), user_participations_path(:period => 'futur'), :class => 'selected') %>
    <% end %>
  </li>
  <li>
    <% if params[:period]!='past'%>
      <%= link_to t('terms.already_started_or_past'), user_participations_path(:period => 'past') %>
    <% else %>
      <%= link_to t('terms.already_started_or_past'), user_participations_path(:period => 'past'), :class => 'selected' %>
    <% end %>
  </li>
</ul>
</div>
