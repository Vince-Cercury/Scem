Dear <%=h get_user_name_or_pseudo(@user) %>,

You receive this email because you are a moderator or admin of an organism.

A picture has been posted

You can eventually:

Suspend the picture (in case it's not respecting the rules):
  <%=h @url_suspend %>

You can see the picture:
 <%=h @url_picture %>

The context of this picture is:
 <%=h @url_context %>

Regards.