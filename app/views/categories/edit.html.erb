<h1><%= t('categories.edit.Edit') %></h1>

<% form_for(@category) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= t('categories.edit.Name') %><br />
    <%= f.text_field :name, :class => 'title' %>
  </p>
  <p>
    <%= t('categories.edit.Description') %><br />
    <%= f.text_area :description, :cols => 35, :rows => 5 %>
  </p>
  <p>
    <%= t('categories.edit.To_display') %><br />
    <%= f.check_box :to_display %>
  </p>
  <p>
    <%= f.submit t('categories.edit.Update') %>
  </p>
<% end %>

<%= link_to t('categories.edit.Show'), @category %> |
<%= link_to t('categories.edit.Back'), categories_path %>
