Dear <%=h get_user_name_or_pseudo(@user) %>,

You receive this email because you are a moderator or admin of an organism.

A comment has been posted

Comment's text:
  <%= @comment.text %>

You can eventually:

Suspend the comment (in case it's not respecting the rules):
  <%=h @url_suspend %>

You can see the comment in its context:
  <%= @parent_object_url %>

Regards.