<% if !facebook_session.user.has_permissions?(['publish_stream','rsvp_event','create_event'])  %>
<h3 class="caps">STEP 1</h3>
<p>We would advise you to let this web site performing some simple actions thanks to your Facebook account.<br />
For that, just click on the following link (you can change your mind and many time):<br />
 <%= fb_prompt_permissions(['publish_stream','rsvp_event','create_event'], 'Improve Facebook permissions') %></p>
 
<h3 class="caps">STEP 2</h3>
<% end %>
<p>Please, confirm your email address. <br />
  Then you will be able to use all the features of the website. (asked just one time)</p>
<% form_for(@user, :url => url_for(:id => @user.id, :action => 'save_email')) do |f| %>
    <%= f.error_messages %>

    <p><%= label_tag 'email' %><br/>
    <%= f.text_field :email %></p>
    <p>
      <%= f.submit 'Update' %>
    </p>
  <% end %>